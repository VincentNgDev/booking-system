{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "schema:generate": "tsx ./scripts/schema.generate.ts",
    "db:generate": "pnpm run schema:generate && zenstack generate",
    "db:push": "prisma db push",
    "db:migrate": "prisma migrate dev --skip-generate",
    "db:deploy": "prisma migrate deploy",
    "db:dev": "pnpm run schema:generate && zenstack generate && prisma db push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.4.1",
  "devDependencies": {
    "@types/node": "^20",
    "prisma": "catalog:prisma",
    "tsx": "^4.19.3",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@prisma/client": "6.6.0",
    "zenstack": "link:local_modules\\zenstack"
  }
}
